name: publish-lvthncli

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: true
        type: choice
        options: [patch, minor, major]

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 'latest'

      - name: setup node (npm registry)
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 'lts/*'
          registry-url: 'https://registry.npmjs.org'

      - name: Verify npm token
        run: npm whoami
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: configure git identity
        run: |
          git config user.name  "leviathan-cicd[bot]"
          git config user.email "leviathan-cicd[bot]@users.noreply.github.com"

      - name: install dependencies
        working-directory: cli
        run: bun install

      - name: run tests
        working-directory: cli
        run: bun check

      - name: bundle workers
        working-directory: cli
        run: |
          mkdir -p src/serpent src/chacha

          bun build node_modules/leviathan-crypto/dist/serpent/pool-worker.js \
            --target browser --format iife --minify \
            --outfile /tmp/serpent-worker.js
          SERPENT_BUNDLE=$(node -e "
            const fs = require('fs');
            process.stdout.write(JSON.stringify(fs.readFileSync('/tmp/serpent-worker.js', 'utf8')));
          ")
          echo "// AUTO-GENERATED by CI: do not edit" > src/serpent/worker-bundle.ts
          echo "export const WORKER_BUNDLE = ${SERPENT_BUNDLE};" >> src/serpent/worker-bundle.ts

          bun build node_modules/leviathan-crypto/dist/chacha20/pool-worker.js \
            --target browser --format iife --minify \
            --outfile /tmp/chacha-worker.js
          CHACHA_BUNDLE=$(node -e "
            const fs = require('fs');
            process.stdout.write(JSON.stringify(fs.readFileSync('/tmp/chacha-worker.js', 'utf8')));
          ")
          echo "// AUTO-GENERATED by CI: do not edit" > src/chacha/worker-bundle.ts
          echo "export const WORKER_BUNDLE = ${CHACHA_BUNDLE};" >> src/chacha/worker-bundle.ts

      - name: cross-compile all platforms
        working-directory: cli
        run: |
          mkdir -p \
            packages/lvthn-linux-x64/bin \
            packages/lvthn-linux-arm64/bin \
            packages/lvthn-darwin-x64/bin \
            packages/lvthn-darwin-arm64/bin \
            packages/lvthn-win32-x64/bin

          targets=(
            "bun-linux-x64    packages/lvthn-linux-x64/bin/lvthn"
            "bun-linux-arm64  packages/lvthn-linux-arm64/bin/lvthn"
            "bun-darwin-x64   packages/lvthn-darwin-x64/bin/lvthn"
            "bun-darwin-arm64 packages/lvthn-darwin-arm64/bin/lvthn"
            "bun-windows-x64  packages/lvthn-win32-x64/bin/lvthn.exe"
          )

          for entry in "${targets[@]}"; do
            target=$(echo $entry | awk '{print $1}')
            outfile=$(echo $entry | awk '{print $2}')
            echo "Compiling $target -> $outfile"
            bun build src/main.ts \
              --compile \
              --target="$target" \
              --outfile="$outfile" \
              --minify
          done

          chmod +x \
            packages/lvthn-linux-x64/bin/lvthn \
            packages/lvthn-linux-arm64/bin/lvthn \
            packages/lvthn-darwin-x64/bin/lvthn \
            packages/lvthn-darwin-arm64/bin/lvthn

          echo "Compiled binaries:"
          ls -lh packages/*/bin/

      - name: bump version
        working-directory: cli
        run: |
          CURRENT=$(node -p "require('./package.json').version")
          NEW=$(node -e "
            const [major, minor, patch] = '$CURRENT'.split('.').map(Number);
            const bump = '${{ inputs.bump }}';
            if (bump === 'major') console.log((major+1) + '.0.0');
            else if (bump === 'minor') console.log(major + '.' + (minor+1) + '.0');
            else console.log(major + '.' + minor + '.' + (patch+1));
          ")
          echo "VERSION=$NEW" >> $GITHUB_ENV
          echo "Bumping $CURRENT -> $NEW"

          # Update root package.json
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '$NEW';
            for (const k of Object.keys(pkg.optionalDependencies || {}))
              pkg.optionalDependencies[k] = '$NEW';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

          # Update all platform package.json versions
          for dir in packages/*/; do
            node -e "
              const fs = require('fs');
              const pkg = JSON.parse(fs.readFileSync('${dir}package.json', 'utf8'));
              pkg.version = '$NEW';
              fs.writeFileSync('${dir}package.json', JSON.stringify(pkg, null, 2) + '\n');
            "
          done

      - name: commit and tag
        run: |
          git add cli/package.json cli/packages/*/package.json
          git commit -m "chore(release): lvthn v${{ env.VERSION }}"
          git tag "lvthn-v${{ env.VERSION }}"
          git push origin main
          git push origin "lvthn-v${{ env.VERSION }}"

      - name: strip devDependencies from published manifest
        working-directory: cli
        run: npm pkg delete devDependencies

      - name: publish platform packages
        working-directory: cli
        run: |
          for dir in packages/*/; do
            echo "Publishing $dir..."
            npm publish "$dir" --access public --provenance
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: attest binaries
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        with:
          subject-path: |
            cli/packages/lvthn-linux-x64/bin/lvthn
            cli/packages/lvthn-linux-arm64/bin/lvthn
            cli/packages/lvthn-darwin-x64/bin/lvthn
            cli/packages/lvthn-darwin-arm64/bin/lvthn
            cli/packages/lvthn-win32-x64/bin/lvthn.exe

      - name: publish root package
        working-directory: cli
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: create github release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Copy binaries with platform-suffixed names — gh release uses
          # the filename as the asset name, so all five must be unique.
          cp cli/packages/lvthn-linux-x64/bin/lvthn    /tmp/lvthn-linux-x64
          cp cli/packages/lvthn-linux-arm64/bin/lvthn  /tmp/lvthn-linux-arm64
          cp cli/packages/lvthn-darwin-x64/bin/lvthn   /tmp/lvthn-darwin-x64
          cp cli/packages/lvthn-darwin-arm64/bin/lvthn /tmp/lvthn-darwin-arm64
          cp cli/packages/lvthn-win32-x64/bin/lvthn.exe /tmp/lvthn-win32-x64.exe

          gh release create "lvthn-v${{ env.VERSION }}" \
            --title "lvthn v${{ env.VERSION }}" \
            --notes "lvthn v${{ env.VERSION }} — Serpent-256 + XChaCha20-Poly1305 file encryption CLI

          Install: \`bun add -g lvthn\` or \`npm install -g lvthn\`

          Platforms: linux-x64, linux-arm64, darwin-x64, darwin-arm64, win32-x64" \
            --verify-tag \
            /tmp/lvthn-linux-x64 \
            /tmp/lvthn-linux-arm64 \
            /tmp/lvthn-darwin-x64 \
            /tmp/lvthn-darwin-arm64 \
            /tmp/lvthn-win32-x64.exe
